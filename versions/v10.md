# FreeLang v10

> 이 문서는 "FreeLang v10"이라는 이름 하나만 다룹니다. 숫자는 이름의 일부입니다.

## 1. 어디에 있나

- 같은 이름의 GitHub 저장소 [`freelang-v10`](https://github.com/kimjindol2025/freelang-v10)은 2026-06-19 19:40(KST)에 만든 **빈 저장소**(0KB)입니다.
- 실제 흔적은 **[`freelang-v9`](https://github.com/kimjindol2025/freelang-v9) 저장소 안**에 있습니다.
  - 2026-04-16 21:40 ~ 23:25(KST) 사이 커밋 12개가 "v10"을 이름으로 씁니다. 예: `3d62817` "🎉 feat: FreeLang v10 완전 완성 (100/100) — 최종 릴리스", `da4b372` "🎉 docs: v10.0.0 릴리스 — README 개편 + 릴리스 노트", `33334aa` "fix: v10.0.1 보안 강화", `a5e280e` "feat: v10.1 Phase 1 — 비동기 I/O 완전 구현".
  - 그 저장소의 `package.json` 판 번호는 `10.0.1`, 설명은 "FreeLang v9/v10 — AI-Native Fullstack Platform".
  - README 제목(4/18에 삭제되기 전): "FreeLang v9 & v10".
- kim27의 `freelang-afj` 문서에도 "v10"이 몇 번 나옵니다(아래 3번). 이 v10 커밋들은 kim27 `freelang-afj` 기록에는 **없습니다**(해당 커밋 번호가 그 저장소에 존재하지 않음).
- `fsm-saas-v10`은 빈 저장소이며 이름만 비슷합니다.

## 2. 무엇으로 만들었나 / 규모

- `freelang-v9` 저장소와 같은 **TypeScript** 해석기 위에서 작업됨(위 커밋들이 그 저장소에 있음). v10만 따로 떼어 센 규모는 **확인 안 됨**. 커밋 `3d62817` 메시지는 "실제 구현: 2,500+ 줄, 신규 함수: 80+개, 테스트: 66개 (모두 통과)"라고 적습니다(그 커밋의 주장).

## 3. 어떤 언어인가

- README(삭제 직전 판)의 설명(원문):

> **v10.0.0** (2026-04-16) 은 **백엔드 + 프론트엔드 + 인프라**를 한 언어로 통합하는 진정한 풀스택 플랫폼입니다.

  표에 적힌 핵심 특징: SQLite 내장, SSE 스트리밍(서버가 계속 데이터를 흘려보내는 방식), 로컬 RAG 검색(TF-IDF), AI 토큰 스트리밍, WebSocket, "72/72 테스트 PASS".
- 문법: `freelang-afj`의 `docs/LANGUAGE-FAULTS.md`와 `docs/CLAUDE.md`가 "v10 문법"이라며 드는 예(원문):

```lisp
;; #21 — v10 let (동작 안 함)
(let [x 1 y 2] (+ x y))
;; #26 — v10 [FUNC] 문법 v11에서 동작 안 함
[FUNC add :params [a b] :body (+ a b)]
```

  즉 **괄호 목록형 + `[FUNC]` 대괄호 블록** 모양입니다.
- `freelang-afj/docs/CLAUDE.md`에는 "포트: 신규 40000-43000 | v10 보호 43000-49999"라는 운영 규칙이 있어, v10으로 돌아가던 서비스가 따로 있었던 것으로 적혀 있습니다(어떤 서비스인지는 **확인 안 됨**).

## 4. 상태

- 별도 저장소는 비어 있고, 코드는 `freelang-v9` 저장소 안에만 있습니다. 그 저장소의 실행 결과는 FreeLang v9 문서 참고(설치는 되나 절대 경로 문제로 실행 실패, 테스트 345 통과/154 실패).
- 마지막 v10 관련 커밋: 2026-04-16 23:25.

## 5. 구별되는 점

- 자기 저장소 없이 **다른 이름(v9) 저장소 안의 하루치 커밋과 판 번호로만** 존재하는 경우입니다.
- 목표로 적힌 것이 "백엔드+프론트엔드+인프라를 한 언어로"라는 **풀스택 플랫폼**입니다.

## 문서에 적힌 주장 (조사자 해석 아님)

- `freelang-afj/docs/PERFORMANCE.md`에는 "### v9 → v10 → v11 진화"라는 제목의 표가 있고 v10 수치를 "렉싱 3.0M, 파싱 400K, 실행 500K, 메모리 95MB"로 적었습니다. 측정 방법은 **확인 안 됨**. 이 제목의 관점은 그 문서의 것입니다.
- `freelang-afj`의 커밋 `97730b11`(2026-04-17 07:34) 메시지: "docs: v11 공식 풀스택 언어로 CLAUDE.md 완전 개선 - **v2~v10 폐기, v11 독점**". 문서(커밋 메시지)가 그렇게 적었다는 사실만 옮깁니다.
